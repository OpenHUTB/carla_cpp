# ToolChain.cmake文件，通常用于定义特定的编译工具链相关的配置信息，
# 比如指定编译器的路径、编译选项、目标平台相关设置等，方便在跨平台编译等场景下统一构建规则。
/ToolChain.cmake 

# 目录名，一般用于存放可执行文件，例如编译生成的二进制文件等会放在这个目录下，方便管理和查找可执行程序。
/bin 

# 目录名，常见用于存放构建过程中产生的临时文件、中间文件以及最终构建生成的目标文件等，
# 是编译构建项目时很重要的一个工作目录。
/build 

# 这个看起来像是安装Carla库的目录，里面可能包含了Carla库相关的头文件、库文件以及其他安装后需要部署的资源，
# 方便其他项目链接使用该库。
/libcarla-install 
