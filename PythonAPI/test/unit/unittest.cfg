# [unittest] 部分（尽管这通常是unittest框架的配置部分，但在这里用于nose2配置）
 
# 指定nose2测试框架要使用的插件
# 在这个例子中，我们使用了nose2的junitxml插件来生成JUnit XML格式的测试报告
plugins = nose2.plugins.junitxml
 
# [junit-xml] 部分是nose2的junitxml插件的配置区域
 
# 配置junitxml插件生成的JUnit XML文件的路径和名称
# 测试报告将被保存为指定路径下的test-results.xml文件
path = test-results.xml
