# *.egg-info 目录通常是由 setuptools 等 Python 打包工具生成的，用于存放与 Python 包相关的元数据信息，
# 例如包的版本、依赖关系、作者等信息，方便对包进行管理、分发以及在其他环境中安装使用。

# build 目录一般是在构建项目（比如将 Python 项目打包成可发布的格式等过程）时，用来存放构建过程中
# 临时生成的一些文件、中间产物等。例如在 Python 项目使用 setup.py 进行构建时，会在这里放置编译后的
# 代码文件（如果有编译需求）、资源文件的整理结果等，构建完成后可以根据实际情况决定是否保留该目录。

# dependencies 这个词本身表示依赖关系。在项目配置文件中（比如 Python 项目的 requirements.txt 或者
# 在一些项目特定的配置文件里定义包依赖的地方），它用于列出当前项目运行或者开发所依赖的其他库、模块等，
# 明确这些依赖有助于确保项目在不同环境中都能正确安装和运行，开发人员可以根据它来安装所需的外部组件。

# dist 目录通常是用来存放项目最终的分发文件的地方。对于 Python 项目而言，可能会在这里生成诸如
#.whl（wheel 格式的二进制安装包）、.tar.gz（源码压缩包等可用于分发的文件格式），方便用户下载并安装到
# 他们的环境中，使其能够使用该项目提供的功能。
